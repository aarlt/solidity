{test 01}
/*
    uint32 one = 1;
    uint32 two = 2;
    uint32 three = two + one;
    assert(1 + three == 1 + one + three - 1);
    assert(true);
*/
    uint32 one = 1;
    uint32 two = 2;
    // assert (one + 1 == two);
    soltest.setChainParams(one + 1, two);
