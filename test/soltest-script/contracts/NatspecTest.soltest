{setup}
    soltest.setChainParams(42);
    soltest.mineBlocks(42);
    soltest.modifyTimestamp(42);
    soltest.addBlock(42);
    soltest.rewindToBlock(42);

{test 01}
    NatspecTest c = new NatspecTest();
    assert(c.add(2, 3) == 5);
    uint a;
    uint b;
    (a, b) = c.multi(1, 2);
    assert(a == 3 && b == 2);

{test 02}
    uint i = 0;
/*
    while (i++ < 1) {
        uint same1 = 0;
    }
    if (true) {
        bar += baz;
    } else {
        uint baz = 10;// never executes
    }
*/