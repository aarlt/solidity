{test 01}
    soltest.setChainParams(42);
    NatspecTest c = new NatspecTest();
    assert(c.add(2, 3) == 5);
    uint a;
    uint b;
    (a, b) = c.multi(1, 2);
    assert(a == 3 && b == 2);

{test 02}
    uint i = 0;
/*
    while (i++ < 1) {
        uint same1 = 0;
    }
    if (true) {
        bar += baz;
    } else {
        uint baz = 10;// never executes
    }
*/